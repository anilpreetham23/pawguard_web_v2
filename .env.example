# PawGuard API configuration — variable names only, no secrets.

# Base URL of the PawGuard backend API (v1).
# Falls back to the Render deployment when not set.
NEXT_PUBLIC_API_BASE_URL=https://pawguard-backend-mqri.onrender.com/api/v1

# Canonical public site URL, used for SEO/sitemap/robots metadata.
# Falls back to a placeholder when not set.
NEXT_PUBLIC_SITE_URL=https://pawguard.example.com

NEXT_PUBLIC_GOOGLE_CLIENT_ID=485887290260-6vshlthcact0390hlr13af80vhv93i5n.apps.googleusercontent.com